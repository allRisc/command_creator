User's Guide
=============

This is an example of how the command creator package might be used to create a simple command

.. literalinclude:: ../../examples/example.py
    :language: python
    :caption: example.py

This will create a command that can be run using ```python example.py --help```` with the following
output:

.. literalinclude:: ../build/_tmp/example.out
    :language: text
    :caption: example.py --help