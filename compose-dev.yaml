services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: allrisc/pydocker:1.0.0
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

